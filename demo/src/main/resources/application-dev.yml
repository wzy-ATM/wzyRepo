#开发环境配置
server:
  #端口
  port: 80
  servlet:
    #项目访问路径过滤
    context-path: /

#数据库配置
spring:
  datasource:
    #新版本的驱动
    driver-class-name: com.mysql.cj.jdbc.Driver
    #url后面的参数需要
    url: jdbc:mysql://47.52.111.195:3306/test?characterEncoding=utf8&useUnicode=true&useSSL=false
    username: root
    password: 556055Asd@
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

limit:
  # 注意冒号后面要有一个空格
  minMoney: 0.02
  maxMoney: 1
  errorMsg: 最小红包${limit.minMoney}元，最大红包${limit.maxMoney}元


